#include "main.h"
#include <stdio.h>

/**
 * main - print its name
 * @argc: arguments count
 * @argv: argument vector
 *
 * Return: (0) always successful
 */

int main(int agrc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
